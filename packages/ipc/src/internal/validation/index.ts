/**
 * Internal validation utilities
 *
 * This module contains internal validation logic.
 * DTO validation schemas are in the dto/ folder (public API).
 */

// Re-export internal validation utilities if needed
// Currently, all validation is DTO-related and will be in dto/
